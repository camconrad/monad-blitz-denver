# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Monad Testnet (this app runs on Monad Testnet)
# Optional: override RPC (default: https://testnet-rpc.monad.xyz)
# NEXT_PUBLIC_MONAD_TESTNET_RPC_URL=https://testnet-rpc.monad.xyz

# Voice backend (Gamma Guide) — use one of:
# 1) Convex (recommended): run `npx convex dev`, then set:
# NEXT_PUBLIC_CONVEX_URL=https://<your-deployment>.convex.cloud
# HTTP actions (voice) are at https://<your-deployment>.convex.site/api/voice
# 2) WebSocket: your own backend
# NEXT_PUBLIC_VOICE_WS_URL=ws://localhost:8080/ws/voice

# Trade WebSocket (market data)
# NEXT_PUBLIC_TRADE_WS_URL=ws://localhost:8080/ws/market

# API endpoints
# NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Optional: Analytics, monitoring
# NEXT_PUBLIC_ANALYTICS_ID=your_id_here

# --- Voice / options: set in Convex dashboard (Settings → Environment Variables) ---
# LLM: Gamma Guide coach (e.g. Gemini)
GEMINI_API_KEY=
# TTS: voice responses (e.g. ElevenLabs)
ELEVEN_LABS_API_KEY=
# Price / metadata: options chain, spot (e.g. CoinGecko)
COINGECKO_API_KEY=
# Optional: ElevenLabs voice ID (default: 21m00Tcm4TlvDq8ikWAM)
# ELEVEN_LABS_VOICE_ID=

# --- WalletConnect (RainbowKit) ---
# Get project ID at https://cloud.reown.com; add your production domain to Allowlist to avoid 403.
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# --- Contract (after deploying GammaGuide) ---
# Set to the GammaGuide address printed by `yarn deploy:contract`, or find the deploy tx at
# https://testnet.monadvision.com (search deployer 0x1433dF88aa130363B523f3f452C05854C0a02084).
# NEXT_PUBLIC_GAMMA_GUIDE_ADDRESS=0x...
